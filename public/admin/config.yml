backend:
  name: git-gateway
  branch: main

media_folder: "public/assets/images"
public_folder: "/assets/images"

collections:
  - name: "blog"
    label: "Блог"
    folder: "content/posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Заголовок", name: "title", widget: "string" }
      - { label: "Дата", name: "date", widget: "datetime" }
      - { label: "Текст", name: "body", widget: "markdown" }

  - name: "portfolio"
    label: "Портфолио / Галерея"
    folder: "content/portfolio"
    create: true
    fields:
      - { label: "Название проекта", name: "title", widget: "string" }
      - { label: "Описание", name: "description", widget: "text" }
      - { label: "Изображения", name: "images", widget: "list", field: { label: "Изображение", name: "image", widget: "image" } }
      - { label: "Видео (YouTube/VK)", name: "video", widget: "string", required: false }

# ────────────────────────────────
# РОЛИ
# ────────────────────────────────
users:
  - email: "89528916189kaa@gmail.com"   # Ты — полный админ
    role: admin
  - email: "googlushkin@gmail.com"      # Тестовый второй аккаунт — редактор
    role: editor

roles:
  editor:
    collections:
      - blog
      - portfolio